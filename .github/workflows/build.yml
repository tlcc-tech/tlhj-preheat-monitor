name: build

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest, windows-latest]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install Wails
        shell: bash
        run: |
          go install github.com/wailsapp/wails/v2/cmd/wails@v2.11.0
          echo "${HOME}/go/bin" >> $GITHUB_PATH

      - name: Compute Version
        id: ver
        shell: bash
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Build (macOS)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          wails build -platform darwin/universal -clean -ldflags "-X main.AppVersion=${{ steps.ver.outputs.version }}"

      - name: Build (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          wails build -platform windows/amd64 -clean -ldflags "-X main.AppVersion=${{ steps.ver.outputs.version }}"
          Copy-Item -Force "build/bin/tlhj-preheat-monitor.exe" "build/bin/新服预约监控-windows-amd64.exe"

      - name: Create DMG (macOS, optional)
        if: matrix.os == 'macos-latest'
        shell: bash
        run: |
          brew install create-dmg
          APP_NAME="新服预约监控"
          APP_PATH="build/bin/${APP_NAME}.app"
          DMG_PATH="build/bin/${APP_NAME}.dmg"
          rm -rf build/dmg
          mkdir -p build/dmg
          cp -R "${APP_PATH}" build/dmg/
          create-dmg --volname "${APP_NAME}" \
            --volicon "${APP_PATH}/Contents/Resources/iconfile.icns" \
            --app-drop-link 300 180 \
            "${DMG_PATH}" \
            build/dmg

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.os }}-build
          path: build/bin/**

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: List dist
        shell: bash
        run: |
          ls -la dist || true
          find dist -maxdepth 3 -type f | sed -n '1,200p'

      - name: Create GitHub Release (tag)
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: false
          body: |
            自动构建产物（Windows/macOS）。
            - Windows：新服预约监控-windows-amd64.exe
            - macOS：新服预约监控.dmg
          files: |
            dist/**/新服预约监控.dmg
            dist/**/新服预约监控-windows-amd64.exe
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
